/* ================================================================
   tts-manifest.js
   ── 社長の自分史 電子ブック ─ 事前生成した読み上げ音声の対応表

   このファイルの役割:
     ・各「読み上げ文」のハッシュ（util.js の ttsHash）と、
       事前生成した高音質MP3ファイルのパスを対応づける。
     ・女性(female)/男性(male) それぞれで持つ。

   仕組み:
     BookController は読み上げ時、これから読む文章を ttsHash で
     ハッシュ化し、この表に一致するMP3があればそれを再生する。
     無ければブラウザ内蔵の音声合成にフォールバックする。
     （文章を管理画面で編集するとハッシュが変わり、その箇所だけ
       自動的に内蔵音声へ切り替わる。）

   生成方法:
     Kokoro TTS（Apache-2.0・無料・商用可）で音声を生成し、
     このファイルと audio/tts/ 以下のMP3を出力する。
     まだ音声を生成していない間は、下の表は空でよい
     （その場合は常にブラウザ内蔵音声で読み上げられる）。
   ================================================================ */
const TTS_MANIFEST = { female: {}, male: {} };
